# -----------------------------------------------------------------------------
# LLM Provider
# -----------------------------------------------------------------------------
# 사용 가능 값: mock, openai
# AI Agent에서 OpenAI를 사용하려면 openai로 변경하고 OPENAI_API_KEY를 설정합니다.
LLM_PROVIDER=mock

# -----------------------------------------------------------------------------
# OpenAI
# -----------------------------------------------------------------------------
# 실제 API Key는 .env에만 입력하고 이 파일에는 작성하지 않습니다.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_VISION_MODEL=gpt-4.1-mini
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=coral

# 번호판 인식 방식: mock(파일명 기반), openai(실제 이미지 분석)
VEHICLE_RECOGNIZER=mock

# -----------------------------------------------------------------------------
# Gemini (선택 사항)
# -----------------------------------------------------------------------------
GEMINI_API_KEY=
GEMINI_MODEL=

# -----------------------------------------------------------------------------
# Ollama (선택 사항)
# -----------------------------------------------------------------------------
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2

# -----------------------------------------------------------------------------
# Frontend / Backend
# -----------------------------------------------------------------------------
BACKEND_API_URL=http://127.0.0.1:8000
REQUEST_TIMEOUT_SECONDS=60

# -----------------------------------------------------------------------------
# Weather Tool
# -----------------------------------------------------------------------------
# 사용 가능 값: mock, live
WEATHER_MODE=mock
OPEN_METEO_BASE_URL=https://api.open-meteo.com
OPEN_METEO_GEOCODING_URL=https://geocoding-api.open-meteo.com

# -----------------------------------------------------------------------------
# Image upload
# -----------------------------------------------------------------------------
MAX_IMAGE_SIZE_MB=10

# -----------------------------------------------------------------------------
# Local PostgreSQL
# -----------------------------------------------------------------------------
# Docker의 aidevs-pgvector 컨테이너 기본 예시입니다.
# psycopg.connect()를 직접 사용하므로 postgresql+psycopg://가 아닌
# postgresql:// 형식을 사용합니다.
DATABASE_URL=postgresql://agent_user:agent_password@localhost:5433/agent_db
